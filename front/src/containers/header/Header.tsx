import Navbar from "../../components/navbar/Navbar"

const Header = () => {

    return(
        <header style={{
            position: 'relative'
        }}>
           <Navbar />
        </header>
    )
}

export default Header